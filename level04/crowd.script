function init(self)
	self.pos = go.get_position()
end

function update(self, dt)
	local main = require "main.main"

	go.set_position(self.pos + main.rotation * 25 * vmath.vector3(0, main.offset, 0))
	go.set_rotation(vmath.quat_rotation_z(main.rotation * 2))
end
